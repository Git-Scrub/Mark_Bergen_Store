<div class="container-fluid">
	<h1>Welcome!</h1>
	<% @products.each_slice(@deck_size) do |product| %>
	  <div class="card-deck-wrapper">
	  <div class="card-deck">
	    <%= render product %>
	  </div>
	</div>
	<% end %>
</div>
