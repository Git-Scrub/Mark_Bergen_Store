<nav class="navbar navbar-light" style="background-color: #e3f2fd;">
  <ul class="nav navbar-nav">
    <li class="nav-item <%= 'active' if current_page?("/store") || current_page?("/") %>">
      <a class="nav-link" href="/store">Home <span class="sr-only">(current)</span></a>
    </li>
    <li class="nav-item <%= 'active' if current_page?("/store/contact") %>">
      <a class="nav-link" href="/store/contact">Contact Us</a>
    </li>
    <li class="nav-item <%= 'active' if current_page?("/store/about_us") %>">
      <a class="nav-link" href="/store/about_us">About</a>
    </li>
	<li class="nav-item <%= 'active' if current_page?("/store/cart") %>">
      <a class="nav-link" href="/store/cart">My Cart</a>
    </li>
	<% if session[:user] %>
	  <li class="nav-item <%= 'active' if current_page?("/store/signout") %>">
        <a class="nav-link" href="/store/sign_out">Sign Out</a>
      </li>
	<% else %>
	  <li class="nav-item <%= 'active' if current_page?("/store/signup") %>">
        <a class="nav-link" href="/store/sign_up">Sign Up</a>
      </li>
	  <li class="nav-item <%= 'active' if current_page?("/store/sign_in") %>">
        <a class="nav-link" href="/store/sign_in">Sign In</a>
      </li>
	<% end %>
  </ul>
  <% if current_page?("/store") || current_page?("/") %>
    <form class="form-inline float-xs-right">
      <span class="col-lg-12 no-caridge-return">
        <span class="button-group">
          <button type="button" class="btn btn-default btn-sm dropdown-toggle" data-toggle="dropdown"><span class="glyphicon glyphicon-cog"></span> <span class="caret"></span></button>
          <ul class="dropdown-menu">
		   <% @product_catagories.each do |product_cat| %>
              <li><input type="checkbox" name="product_cat_list[]" value="<%=product_cat.id%>"/><%=product_cat.name%></li>
			<% end %>
          </ul> 
        </span>
        <input class="form-control" type="text" name="search" placeholder="Search">
        <button class="btn btn-outline-success" type="submit" value="search">Search</button>
	  </span>
    </form>
  <% end %>
</nav>